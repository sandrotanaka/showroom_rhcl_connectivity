---
# 2.1 PULA: aplica mTLS STRICT no namespace.
- name: "2.1 — pula (mTLS STRICT)"
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: "Aplicar a PeerAuthentication da amostra"
      kubernetes.core.k8s:
        state: present
        src: "{{ lookup('env','HOME') }}/rhcl/samples/bookinfo/10-mesh-peerauthentication.yaml"
