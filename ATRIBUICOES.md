# Imagens de terceiros neste diretório

| Arquivo | Origem | Licença |
| --- | --- | --- |
| `istio-noistio.svg` | [istio.io/docs/examples/bookinfo](https://istio.io/latest/docs/examples/bookinfo/) (`content/en/docs/examples/bookinfo/noistio.svg`, branch `release-1.31`) | Apache 2.0, © Istio Authors |
| `istio-withistio.svg` | idem (`withistio.svg`) | Apache 2.0, © Istio Authors |

Reproduzidas **sem alteração**. O prefixo `istio-` no nome do arquivo é o que
separa, num `ls`, o que é de terceiro do que foi desenhado aqui
(`topologia.svg`, `camadas.svg`).

Ao atualizar: as duas trazem `width`/`height` explícitos no elemento `<svg>` —
sem isso o Antora as renderiza a 271x150. Conferir depois de qualquer troca.
